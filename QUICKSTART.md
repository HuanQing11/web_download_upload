# 快速开始指南 🚀

## 📋 目录
1. [环境配置](#环境配置)
2. [初始化数据库](#初始化数据库)
3. [运行应用](#运行应用)
4. [新用户指南](#新用户指南)
5. [管理员指南](#管理员指南)

---

## 环境配置

### 步骤1：克隆项目
```bash
git clone <repository-url>
cd web_download_upload
```

### 步骤2：创建虚拟环境（推荐）
```bash
# Windows
python -m venv venv
venv\Scripts\activate

# macOS / Linux
python3 -m venv venv
source venv/bin/activate
```

### 步骤3：安装依赖
```bash
pip install -r requirements.txt
```

---

## 初始化数据库

### 首次运行时执行
```bash
python init_db.py
```

**输出示例：**
```
🔄 正在初始化数据库...
✅ 数据库创建成功: app.db
✅ 目录创建/检查完成: uploads
✅ 目录创建/检查完成: delete

🎉 数据库初始化完成！
📝 提示：第一个注册的用户将自动成为管理员
🚀 现在可以运行: python app.py
```

---

## 运行应用

```bash
python app.py
```

打开浏览器访问：**http://localhost:5000**

---

## 新用户指南

### 1️⃣ 注册账号
- 点击首页的"注册"按钮
- 填写用户名、邮箱、密码
- **⭐ 第一个注册的用户自动成为管理员！**

### 2️⃣ 登录系统
- 使用用户名或邮箱登录
- 支持"记住登录状态"（30天有效期）

### 3️⃣ 上传文件
- 进入"上传文件"页面
- 选择 `.7z` 或 `.zip` 格式的文件
- 点击上传

### 4️⃣ 管理文件
- 在主页查看所有已上传的文件
- **下载**：点击文件名下载
- **删除**：点击删除按钮，文件移至回收站

### 5️⃣ 登出
- 点击右上角"登出"按钮

---

## 管理员指南

### 🔐 获得管理员权限
**注意**：只有第一个注册的用户会自动成为管理员！

### 📊 管理员后台 (`/admin`)
以管理员身份登录后，可访问以下功能：

#### 📋 用户管理
- **路径**：`/admin_user`
- **功能**：
  - 👁️ 查看所有注册用户
  - 🔍 搜索特定用户
  - 🗑️ 删除用户账号

#### 🗑️ 回收站管理（新增！）
- **路径**：`/admin/trash`
- **功能**：
  - 📁 查看所有已删除文件
  - 🔄 恢复文件到用户文件夹
  - 🗑️ 永久删除文件
  - 🧹 清空所有回收站内容

#### 🔎 用户搜索
- **路径**：`/admin/find`
- **功能**：按用户名搜索用户详情

#### ⚠️ 危险操作
- **路径**：`/admin/delete_all`
- **功能**：清空所有用户数据库（谨慎使用！）

---

## 📁 文件存储说明

### 文件上传
```
uploads/
├── 1_admin/          # 用户文件夹（ID_用户名）
│   ├── file1.zip
│   └── file2.7z
└── 2_user1/
    └── document.zip
```

### 回收站
```
delete/
├── 1_admin/          # 用户回收站文件夹
│   ├── 1_file1.zip   # 带版本号的已删除文件
│   └── 2_file2.7z
└── 2_user1/
    └── 1_document.zip
```

**版本号说明**：防止同名文件冲突，格式为 `版本号_原文件名`

---

## ⚙️ 数据库配置

### 默认配置（SQLite）
开箱即用，无需配置数据库。

### 切换到MySQL（可选）
如需使用MySQL数据库：

#### 1. 修改 `config.py`
```python
DB_TYPE = 'mysql'
```

#### 2. 安装MySQL驱动
```bash
pip install mysqlclient
```

#### 3. 创建数据库
```sql
CREATE DATABASE database_learn DEFAULT CHARACTER SET utf8mb4;
```

#### 4. 重新初始化
```bash
python init_db.py
```

---

## 🐛 常见问题

### Q: 第一个注册的用户如何成为管理员？
A: 自动！在新数据库中，第一个注册的用户会自动被设置为管理员。

### Q: 如何重置管理员？
A: 删除 `app.db` 后重新运行 `init_db.py`，然后以新用户身份重新注册。

### Q: 上传失败，提示"仅允许上传压缩包"？
A: 请确保上传的文件格式为 `.7z` 或 `.zip`。其他格式暂不支持。

### Q: 如何查看删除的文件？
A: 以管理员身份登录，进入 `/admin/trash` 查看所有用户的回收站文件。

### Q: 怎样永久删除回收站文件？
A: 在 `/admin/trash` 页面，选择文件点击"永久删除"按钮。

### Q: 能恢复已永久删除的文件吗？
A: 不能。永久删除后文件无法恢复，请谨慎操作。

---

## 📚 更多信息

- 📖 详细文档：见 `README.md`
- 🔗 项目源码：见各 `.py` 文件
- 🌐 框架文档：[Flask官方文档](https://flask.palletsprojects.com/)

---

**祝您使用愉快！** 🎉

如有问题或建议，欢迎反馈！
